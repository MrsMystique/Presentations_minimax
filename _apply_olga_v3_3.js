// Применение правок Olga v3.3: новые картинки + удаление эмодзи + ловушка соль/вода
const fs = require('fs');
const p = 'Чистовые слайды v3.3 — Проценты, пропорции, масштаб.md';
let c = fs.readFileSync(p, 'utf8');

// === 1. Заменить старую картинку stavki-sravnenie.png на новую vsplysk-protsent.png ===
const oldStavki = '![Сравнение ставок: капитализация — простой процент (прямая 50000 → 62000) vs сложный процент (кривая 50000 → 62985) за 5 лет, разница 985 руб](assets/stavki-sravnenie.png)';
const newStavki = '![Закон роста всплеска: 50000 руб под 8% на 3 года — сложный процент (кривая) = 62985,6 vs простой процент (прямая) = 62000, разница +985,6 руб](assets/vsplysk-protsent.png)';
if (c.indexOf(oldStavki) >= 0) {
  c = c.replace(oldStavki, newStavki);
  console.log('✓ Слайд 17: stavki-sravnenie.png → vsplysk-protsent.png (закон роста всплеска)');
}

// === 2. Добавить картинку balans-proportsiya.png на Слайд 3 (Пропорция) ===
const slide3Old = `### Слайд 3. Пропорция: равенство двух отношений

**Почему это важно?**`;
const slide3New = `### Слайд 3. Пропорция: равенство двух отношений

![Закон абсолютного баланса: a/b = c/d — произведения наискосок равны (a·d = b·c). Два прямоугольника: 2/3 = 4/6](assets/balans-proportsiya.png)

**Почему это важно?**`;
if (c.indexOf(slide3Old) >= 0) {
  c = c.replace(slide3Old, slide3New);
  console.log('✓ Слайд 3: добавлен закон абсолютного баланса');
}

// === 3. Использовать shpory-mozhno.png на Слайде 23 (Мотивация перед экзаменом) ===
const motivOld = `### Слайд 23. Зачем тебе всё это: мотивация перед экзаменом

**Ты только что собрал полный арсенал.**`;
const motivNew = `### Слайд 23. Зачем тебе всё это: мотивация перед экзаменом

![Можно, это шпора! Хитрый граф подглядывает — но легенда разрешает использовать шпаргалки как инструмент](assets/shpory-mozhno.png)

**Ты только что собрал полный арсенал.**`;
if (c.indexOf(motivOld) >= 0) {
  c = c.replace(motivOld, motivNew);
  console.log('✓ Слайд 23: добавлена картинка «Можно, это шпора»');
}

// === 4. Добавить финальный слайд (после Слайда 24.9) ===
const finalOld = `### Слайд 24.9. Квартиры в двух домах (баланс через переменные)

**Условие.`;
const finalNew = `### Слайд 24.9. Квартиры в двух домах (баланс через переменные)

**Условие.`;
if (c.indexOf(finalOld) >= 0) {
  // Найдём конец Слайда 24.9
  const slide249Start = c.indexOf('### Слайд 24.9.');
  const slide249End = c.length;
  // Вставим Слайд 24.10 после Слайда 24.9
  const finaleBlock = `

---

### Слайд 24.10. Финал партии: «Теперь ты слишком много знаешь»

![Финальный слайд партии №3: Subject G-07 в темноте — «Теперь ты слишком много знаешь. Тебя придётся убрать...»](assets/finalnyy-slayd.png)

> **Граф-помощник:** «Опять этот пафос...»
>
> **Что это значит для тебя:** ты прошёл всю партию. Пропорция, зависимости, масштаб, проценты всех типов, сложный процент и десять ловушек. Теперь задачи на проценты — твоя территория.
>
> **Что дальше:** в следующей партии мы построим на этом фундаменте задачи на движение, работу и смеси — там без пропорции никуда.
>
> Если готов — двигаемся. Если хочешь вернуться и прокачать что-то конкретное — Блок 8 (Слайды 24-24.9) всегда открыт.
`;
  c = c.substring(0, slide249End) + finaleBlock;
  console.log('✓ Добавлен Слайд 24.10: Финал партии');
}

// === 5. Обновить TOC: добавить 24.10 ===
const tocFixOld = '- Слайд 24.9: Квартиры Альфа/Омега';
const tocFixNew = '- Слайд 24.9: Квартиры Альфа/Омега\n- Слайд 24.10: Финал партии';
if (c.indexOf(tocFixOld) >= 0 && c.indexOf(tocFixNew) < 0) {
  c = c.replace(tocFixOld, tocFixNew);
  console.log('✓ TOC обновлён: добавлен Слайд 24.10');
}

// === 6. Убрать дешёвые эмодзи (Olga: "выглядят дешево") ===
// Заменяем на текстовые метки (которые Olga попросила убрать — но оставляем как основу, чтобы текст не превратился в кашу)
const emojiReplacements = [
  // 💡 Лайфхак → Лайфхак (без эмодзи)
  ['> 💡 Лайфхак:', '> **Лайфхак:**'],
  // 🔑 Правило → Правило
  ['> 🔑 **Правило:**', '> **Правило:**'],
  // 🚨 Возможная ловушка → Возможная ловушка
  ['> 🚨 Возможная ловушка:', '> **Возможная ловушка:**'],
  ['> ⚠️ Возможная ловушка:', '> **Возможная ловушка:**'],
  // 🔍 Как думать → Как думать
  ['**🔍 Как думать', '**Как думать'],
  // 🛠️ Практический приём → Практический приём
  ['> 🛠️ **Практический приём:**', '> **Практический приём:**'],
  // 🎯 — оставляю (это не эмодзи-маркер, а часть повествования)
  // ❌ — оставляю как маркер ошибки (это математический знак, не дешёвый эмодзи)
  // ✓ — оставляю (галочка — математический знак, не дешёвый эмодзи, Olga сама разрешила)
  // 🚨 в шапке мостиков — оставляю пока, посмотрим
  // > 🎯 — убираю
  ['> 🎯 Поздравляю:', '> **Поздравляю:**'],
  ['> 🎯 **Поздравляю:**', '> **Поздравляю:**'],
  // 🧱 Твой первый кирпич
  ['> 🧱 **Твой первый кирпич партии готов.**', '> **Первый кирпич партии готов.**'],
  // 🔎
  ['> 🔎 **Откуда берётся множитель', '> **Откуда берётся множитель'],
  // 🛠 Практический приём в тексте
  ['> 🛠 Практический приём:', '> **Практический приём:**'],
  // 🪞
  ['**🪞 Интуиция', '**Интуиция'],
];

let emojisApplied = 0;
for (const [from, to] of emojiReplacements) {
  if (c.indexOf(from) >= 0) {
    c = c.replace(from, to);
    emojisApplied++;
    console.log('✓ Emoji removed: ' + from.substring(0, 40));
  }
}
console.log('Emoticons removed: ' + emojisApplied);

// === 7. Добавить ловушку про соль/воду в Слайд 15.2 (Смеси) ===
const slide152Old = `### Слайд 15.2. Задачи на смеси и сплавы (растворы)

![Смеси и сплавы: закон суммы долей — два раствора 10% и 20% сливаются, формула m₁ + m₂ = const](assets/smesi-splavy.png)`;
const slide152New = `### Слайд 15.2. Задачи на смеси и сплавы (растворы)

![Смеси и сплавы: закон суммы долей — два раствора 10% и 20% сливаются, формула m₁ + m₂ = const](assets/smesi-splavy.png)

> **Возможная ловушка:** не путай «процент соли» с «процентом воды». В растворе соли \(8\%\) означает \(8\%\) массы — это соль. Остальные \(92\%\) — вода. Если в задаче спрашивают «процент воды», ответ будет \(92\%\), а не \(8\%\). Всегда смотри, ЧТО именно в процентах: соль, вода, кислота, спирт, жир, примесь.`;
if (c.indexOf(slide152Old) >= 0) {
  c = c.replace(slide152Old, slide152New);
  console.log('✓ Слайд 15.2: добавлена ловушка соль/вода');
}

fs.writeFileSync(p, c, 'utf8');
console.log('\n=== ALL APPLIED ===');
