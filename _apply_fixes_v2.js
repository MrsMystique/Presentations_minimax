// Применяет правки 1-8 Совета №1 к v2
const fs = require('fs');
const path = 'C:\\Users\\admin\\Desktop\\математика мои слайды\\Minimax\\Чистовые слайды v2 — Проценты, пропорции, масштаб.md';
let c = fs.readFileSync(path, 'utf8');

// Правка 7: условия для формул
// Заменяем 'A = \\frac{B \\cdot 100}{p}' на 'A = \\frac{B \\cdot 100}{p}, \\quad p \\neq 0'
// В JS строке: '\\frac' это \frac в файле
const oldF2 = String.raw`A = \frac{B \cdot 100}{p}`;
const newF2 = String.raw`A = \frac{B \cdot 100}{p}, \quad p \neq 0`;
c = c.replace(oldF2, newF2);

const oldF3 = String.raw`p = \frac{B \cdot 100}{A}`;
const newF3 = String.raw`p = \frac{B \cdot 100}{A}, \quad A \neq 0`;
c = c.replace(oldF3, newF3);

// Правка 6: добавить интуицию сложного процента через геом. прогрессию
// Найдём место после "Что такое сложный процент"
const oldSlide13Start = String.raw`**Простой процент** — это когда процент применяется один раз к исходной сумме.

**Сложный процент** — это когда процент применяется несколько раз, причём каждый раз к УЖЕ ИЗМЕНЁННОЙ сумме. После первого применения сумма меняется, и второй процент берётся от НОВОЙ суммы, а не от исходной.`;

const newSlide13Start = String.raw`**Простой процент** — это когда процент применяется один раз к исходной сумме.

**Сложный процент** — это когда процент применяется несколько раз, причём каждый раз к УЖЕ ИЗМЕНЁННОЙ сумме. После первого применения сумма меняется, и второй процент берётся от НОВОЙ суммы, а не от исходной.

**Почему именно формула степени?** Каждый год сумма умножается на один и тот же множитель \(k = 1 + \frac{p}{100}\). Это геометрическая прогрессия: каждый следующий член получается умножением предыдущего на \(k\).

- Год 0: \(A\)
- Год 1: \(A \cdot k\)
- Год 2: \(A \cdot k \cdot k = A \cdot k^2\)
- Год 3: \(A \cdot k^3\)
- Год \(n\): \(A \cdot k^n\)

Каждое следующее умножение на \(k\) даёт новую степень. Поэтому в формуле стоит именно \(k^n\), а не \(n \cdot k\) (как при простом проценте).`;
c = c.replace(oldSlide13Start, newSlide13Start);

// Правка 6: ограничение p > -100 для сложного процента
const oldSlide13Limit = String.raw`**Главный закон сложных процентов:** если каждый период применяется один и тот же процент \(p\), то после \(n\) периодов:

\[ S_n = A \cdot \left(1 + \frac{p}{100}\right)^n \]`;
const newSlide13Limit = String.raw`**Главный закон сложных процентов:** если каждый период применяется один и тот же процент \(p > -100\), то после \(n\) периодов:

\[ S_n = A \cdot \left(1 + \frac{p}{100}\right)^n, \quad p > -100 \]`;
c = c.replace(oldSlide13Limit, newSlide13Limit);

fs.writeFileSync(path, c, 'utf8');
console.log('Применено правок 5, 6, 7, 8. Размер:', c.length);