// Правки Совета v3.3 → v3.4: 5 точечных улучшений
const fs = require('fs');
const p = 'Чистовые слайды v3.3 — Проценты, пропорции, масштаб.md';
let c = fs.readFileSync(p, 'utf8');
let a = 0;

// === Правка #1: Слайд 2 — «39 слайдов» → «40 слайдов» ===
const oldCount = 'Эта партия состоит из 39 слайдов в 8 блоках + 6 мостиков.';
const newCount = 'Эта партия состоит из 40 слайдов в 8 блоках + 6 мостиков.';
if (c.indexOf(oldCount) >= 0) {
  c = c.replace(oldCount, newCount);
  a++;
  console.log('✓ #1 Слайд 2: 39 → 40');
}

// === Правка #3: Слайд 15.2 — добавить «Почему это важно» + «Как думать» ===
const slide152Old = `### Слайд 15.2. Задачи на смеси и сплавы (растворы)

![Смеси и сплавы: закон суммы долей — два раствора 10% и 20% сливаются, формула m₁ + m₂ = const](assets/smesi-splavy.png)`;
const slide152New = `### Слайд 15.2. Задачи на смеси и сплавы (растворы)

**Почему это важно?** Задачи на растворы, сплавы и смеси — обязательный тип на ЦТ и ЕГЭ. В быту это бытовые растворы (соль, уксус), в химии — концентрация, в экономике — купажирование. Умение видеть в задаче три сущности (масса вещества, масса растворителя, процент) — это навык, который переносится на любую задачу про «сколько одного в другом».

![Смеси и сплавы: закон суммы долей — два раствора 10% и 20% сливаются, формула m₁ + m₂ = const](assets/smesi-splavy.png)`;
if (c.indexOf(slide152Old) >= 0) {
  c = c.replace(slide152Old, slide152New);
  a++;
  console.log('✓ #3 Слайд 15.2: добавлено «Почему это важно»');
}

// Добавить «Как думать» в конец Слайда 15.2
const slide152EndOld = `> **Возможная ловушка:** не путай «процент соли» с «процентом воды». В растворе соли \\(8\\%\\) означает \\(8\\%\\) массы — это соль. Остальные \\(92\\%\\) — вода. Если в задаче спрашивают «процент воды», ответ будет \\(92\\%\\), а не \\(8\\%\\). Всегда смотри, ЧТО именно в процентах: соль, вода, кислота, спирт, жир, примесь.`;
const slide152EndNew = slide152EndOld + `

**Как думать при новой задаче на смеси:**

1. **Найди, что считаем «целым»** — масса раствора или масса сплава. Всегда в килограммах или граммах (не в процентах).
2. **Найди «часть»** — масса чистого вещества (соли, кислоты, металла). Это масса × процент.
3. **Запиши равенство:** масса_части_1 + масса_части_2 = масса_части_общая. Масса_целого_1 + масса_целого_2 = масса_целого_общая.
4. **Примени формулу процента:** новый_процент = (общая_часть × 100) / общее_целое.`;
if (c.indexOf(slide152EndOld) >= 0) {
  c = c.replace(slide152EndOld, slide152EndNew);
  a++;
  console.log('✓ #3 Слайд 15.2: добавлен блок «Как думать»');
}

// === Правка #5: «Как думать» в Слайды 12, 14, 15, 24.4, 24.8 ===
// Слайд 12 (Тип 1: процент от числа)
const slide12HowThink = `
**Как думать при новой задаче «найти процент от числа»:**

1. **Найди целое** (то, от чего считаем) и **процент**.
2. **Запиши формулу:** часть = целое × (процент / 100).
3. **Подставь и считай.** Если процент дробный (12,5%) — умножай на десятичную дробь (0,125).`;
if (c.indexOf('### Слайд 12.') >= 0 && c.indexOf('**Как думать при новой задаче «найти процент от числа»:**') < 0) {
  // Найдём конец Слайда 12
  const idx12 = c.indexOf('### Слайд 12.');
  const idx13 = c.indexOf('### Слайд 13.');
  if (idx12 >= 0 && idx13 >= 0) {
    const slide12 = c.substring(idx12, idx13);
    // Добавим блок в конец
    const newSlide12 = slide12 + '\n' + slide12HowThink;
    c = c.substring(0, idx12) + newSlide12 + c.substring(idx13);
    a++;
    console.log('✓ #5 Слайд 12: добавлен блок «Как думать»');
  }
}

// Слайд 24.8 (Переливание)
const slide248HowThink = `
**Как думать при новой задаче на переливание:**

1. **Определи направление** переливания: из какого сосуда в какой.
2. **Запиши, сколько процентов** ушло из одного сосуда и сколько пришло в другой.
3. **Запиши равенство** массы или объёма в каждом сосуде после переливания.`;
if (c.indexOf('### Слайд 24.8.') >= 0 && c.indexOf('**Как думать при новой задаче на переливание:**') < 0) {
  const idx248 = c.indexOf('### Слайд 24.8.');
  const idx249 = c.indexOf('### Слайд 24.9.');
  if (idx248 >= 0 && idx249 >= 0) {
    const slide248 = c.substring(idx248, idx249);
    const newSlide248 = slide248 + '\n' + slide248HowThink;
    c = c.substring(0, idx248) + newSlide248 + c.substring(idx249);
    a++;
    console.log('✓ #5 Слайд 24.8: добавлен блок «Как думать»');
  }
}

// Слайд 24.4 (Прямая vs обратная зависимость)
const slide244HowThink = `
**Как думать при новой задаче на зависимость:**

1. **Пойми смысл задачи:** что происходит с одной величиной, когда другая растёт? Больше → больше (прямая) или больше → меньше (обратная)?
2. **Запиши пропорцию** по выбранному типу. Прямая: \(A_1/B_1 = A_2/B_2\). Обратная: \(A_1/B_2 = A_2/B_1\) (одна колонка перевёрнута).
3. **Реши через закон наискосок.**`;
if (c.indexOf('### Слайд 24.4.') >= 0 && c.indexOf('**Как думать при новой задаче на зависимость:**') < 0) {
  const idx244 = c.indexOf('### Слайд 24.4.');
  const idx245 = c.indexOf('### Слайд 24.5.');
  if (idx244 >= 0 && idx245 >= 0) {
    const slide244 = c.substring(idx244, idx245);
    const newSlide244 = slide244 + '\n' + slide244HowThink;
    c = c.substring(0, idx244) + newSlide244 + c.substring(idx245);
    a++;
    console.log('✓ #5 Слайд 24.4: добавлен блок «Как думать»');
  }
}

// === Правка #2: Сборник формул — добавить мини-якоря ===
// Слайд 18 (Шпаргалка пропорции)
const slide18Add = `

**Когда применять:** любые задачи с двумя парами связанных величин (больше/меньше в одной колонке — больше/меньше в другой, или наоборот).`;
if (c.indexOf('### Слайд 18.') >= 0 && c.indexOf('**Когда применять:**') < 0) {
  const idx18 = c.indexOf('### Слайд 18.');
  const idx19 = c.indexOf('### Слайд 19.');
  if (idx18 >= 0 && idx19 >= 0) {
    const slide18 = c.substring(idx18, idx19);
    const newSlide18 = slide18 + '\n' + slide18Add;
    c = c.substring(0, idx18) + newSlide18 + c.substring(idx19);
    a++;
    console.log('✓ #2 Слайд 18: добавлен якорь «Когда применять»');
  }
}

// Слайд 19 (Шпаргалка зависимостей) — якорь
const slide19Add = `

**Когда применять:** задачи, где две величины двигаются вместе (рабочие/время, скорость/путь, цена/количество).`;
if (c.indexOf('### Слайд 19.') >= 0 && c.indexOf('рабочие/время') < 0) {
  const idx19 = c.indexOf('### Слайд 19.');
  const idx20 = c.indexOf('### Слайд 20.');
  if (idx19 >= 0 && idx20 >= 0) {
    const slide19 = c.substring(idx19, idx20);
    const newSlide19 = slide19 + '\n' + slide19Add;
    c = c.substring(0, idx19) + newSlide19 + c.substring(idx20);
    a++;
    console.log('✓ #2 Слайд 19: добавлен якорь');
  }
}

// Слайд 20 (Шпаргалка процентов)
const slide20Add = `

**Когда применять:** скидки, налоги, изменение цены, рост населения, наценки — везде, где в задаче есть слово «процент» или знак \(\%\).`;
if (c.indexOf('### Слайд 20.') >= 0 && c.indexOf('наценки — везде') < 0) {
  const idx20 = c.indexOf('### Слайд 20.');
  const idx21 = c.indexOf('### Слайд 21.');
  if (idx20 >= 0 && idx21 >= 0) {
    const slide20 = c.substring(idx20, idx21);
    const newSlide20 = slide20 + '\n' + slide20Add;
    c = c.substring(0, idx20) + newSlide20 + c.substring(idx21);
    a++;
    console.log('✓ #2 Слайд 20: добавлен якорь');
  }
}

// Слайд 21 (Шпаргалка масштаба)
const slide21Add = `

**Когда применять:** карты, чертежи, модели, планы квартир — везде, где есть отношение расстояния на рисунке к реальному.`;
if (c.indexOf('### Слайд 21.') >= 0 && c.indexOf('планы квартир') < 0) {
  const idx21 = c.indexOf('### Слайд 21.');
  const idx22 = c.indexOf('### Слайд 22.');
  if (idx21 >= 0 && idx22 >= 0) {
    const slide21 = c.substring(idx21, idx22);
    const newSlide21 = slide21 + '\n' + slide21Add;
    c = c.substring(0, idx21) + newSlide21 + c.substring(idx22);
    a++;
    console.log('✓ #2 Слайд 21: добавлен якорь');
  }
}

// Слайд 22 (Шпаргалка сложных процентов)
const slide22Add = `

**Когда применять:** банковские вклады с капитализацией, инфляция, ежегодный рост показателя, инвестиции с реинвестированием.`;
if (c.indexOf('### Слайд 22.') >= 0 && c.indexOf('инвестиции с реинвестированием') < 0) {
  const idx22 = c.indexOf('### Слайд 22.');
  const idx23 = c.indexOf('### Слайд 23.');
  if (idx22 >= 0 && idx23 >= 0) {
    const slide22 = c.substring(idx22, idx23);
    const newSlide22 = slide22 + '\n' + slide22Add;
    c = c.substring(0, idx22) + newSlide22 + c.substring(idx23);
    a++;
    console.log('✓ #2 Слайд 22: добавлен якорь');
  }
}

console.log('\nTotal applied: ' + a);
fs.writeFileSync(p, c, 'utf8');
