// Применяет правку 4 к v2.2: Слайд 17 — числовой дубль + разбить длинную строку
const fs = require('fs');
const path = require('path');

const filePath = path.join(
  'C:\\Users\\admin\\Desktop\\математика мои слайды\\Minimax',
  'Чистовые слайды v2.2 — Проценты, пропорции, масштаб.md'
);

let content = fs.readFileSync(filePath, 'utf8');

// ВНИМАНИЕ: В файле строки 438/439 имеют \frac как 'rac' из-за PowerShell \f → form feed.
// Поэтому используем '\f' как одиночный символ для соответствия.
// Но для замены используем точную строку из файла (с 'rac').
const oldBlock = [
  '### Слайд 17. Шпаргалка процентов',
  '',
  '| Тип задачи | Формула | Пример |',
  '| --- | --- | --- |',
  '| Часть от целого | \\(\\text{Часть} = A \\cdot \\frac{p}{100}\\) | \\(15\\%\\) от \\(200\\) = 30 |',
  '| Целое по части | \\(A = \\frac{B \\cdot 100}{p} \\Rightarrow A = \\frac{30 \\cdot 100}{15} = 200\\) | 30 это \\(15\\%\\) \\(\\to A = 200\\) |',
  '| Процент по части | \\(p = rac{B \\cdot 100}{A}\\) | 30 от 200 = \\(15\\%\\) |',
  '| Изменение в \\(\\%\\) | \\(p = rac{(B - A) \\cdot 100}{A}\\) | Цена \\(100 \\to 120\\) = \\(+20\\%\\) |'
].join('\n');

const newBlock = [
  '### Слайд 17. Шпаргалка процентов',
  '',
  '| Тип задачи | Формула | Пример |',
  '| --- | --- | --- |',
  '| Часть от целого | \\(\\text{Часть} = A \\cdot \\frac{p}{100}\\) | \\(15\\%\\) от \\(200\\):<br>\\(\\text{Часть} = 200 \\cdot \\frac{15}{100} = 30\\) |',
  '| Целое по части | \\(A = \\frac{B \\cdot 100}{p}\\) |<br>Подставляем \\(B = 30, p = 15\\):<br>\\(A = \\frac{30 \\cdot 100}{15} = 200\\) | 30 это \\(15\\%\\) \\(\\to A = 200\\) |',
  '| Процент по части | \\(p = \\frac{B \\cdot 100}{A}\\) | 30 от 200 = \\(15\\%\\) |',
  '| Изменение в \\(\\%\\) | \\(p = \\frac{(B - A) \\cdot 100}{A}\\) | Цена \\(100 \\to 120\\) = \\(+20\\%\\) |'
].join('\n');

if (content.indexOf(oldBlock) === -1) {
  console.log('OLD BLOCK NOT FOUND');
  process.exit(1);
}

content = content.replace(oldBlock, newBlock);
fs.writeFileSync(filePath, content, 'utf8');
console.log('OK: правка 4 применена');