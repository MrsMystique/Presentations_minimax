#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import io
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')

FILE = r"C:\Users\admin\Desktop\математика мои слайды\Minimax\Геометрия_11_Полный_гайд.md"

with io.open(FILE, 'r', encoding='utf-8') as f:
    content = f.read()

# ============================================================
# FIX: Добавить дерево решений "вписанная vs описанная сфера"
# Вставить после секции 14.7 (микро-проверка), перед 14.8 (мостик)
# ============================================================

OLD_MARKER = "## 14.8. Мостик к секции 15"
NEW_INSERT = """## 14.7a. Дерево решений: «вписанная vs описанная сфера» (для задач ЦТ)

**Алгоритм выбора формулы (шаг за шагом):**

**Шаг 1. Прочитай условие и найди ключевую фразу.**

| Фраза в условии | Что это значит | Что искать |
|---|---|---|
| «сфера проходит через **все вершины** многогранника» | **Описанная** сфера | Радиус \(R\) |
| «сфера **касается всех граней** изнутри» | **Вписанная** сфера | Радиус \(r\) |
| «шар вписан в куб» | Шар касается всех 6 граней | \(r = a/2\) |
| «шар описан около куба» | Шар проходит через все 8 вершин | \(R = a\\sqrt{3}/2\) |
| «шар вписан в конус» | Касается основания и боковой поверхности | \(r = hR/(R + l)\) |
| «шар описан около конуса» | Проходит через вершину и окружность основания | \(R_{\\text{сф}} = l^2/(2h)\) |

**Шаг 2. Проверь базу (для куба и тетраэдра).**

| Тело | \(R\) (описанная) | \(r\) (вписанная) | Отношение |
|---|---|---|---|
| Куб с ребром \(a\) | \(\\dfrac{a\\sqrt{3}}{2}\) | \(\\dfrac{a}{2}\) | \(R = r\\sqrt{3}\) |
| Правильный тетраэдр с ребром \(a\) | \(\\dfrac{a\\sqrt{6}}{4}\) | \(\\dfrac{a\\sqrt{6}}{12}\) | \(R = 3r\) |

**Шаг 3. Если фигура сложнее (призма, пирамида, цилиндр, конус):**

1. Найди **центр** сферы на оси симметрии (для прямой призмы, правильной пирамиды, конуса, цилиндра).
2. Запиши два уравнения:
   - расстояние от центра до **вершины** (или точки на окружности) = \(R\) или \(r\);
   - расстояние от центра до **плоскости грани** (для вписанной) или до **основания** (для описанной) = координата.
3. Реши систему уравнений.

**Шаг 4. Типичные ловушки ЦТ.**

1. **«Сфера вписана в куб» ≠ «Сфера описана около куба».** В первом случае \(r = a/2\), во втором \(R = a\\sqrt{3}/2\). Это **разные радиусы**, разница в \(\\sqrt{3}\) раз.
2. **«Шар касается рёбер»** — это НЕ вписанная сфера. Вписанная сфера касается **граней**, не рёбер.
3. **Если в условии «плоскость касается шара»** — это значит расстояние от центра шара до плоскости = \(R\).
4. **Если «шар пересекает плоскость по окружности»** — расстояние от центра до плоскости < \(R\). Радиус сечения = \(\\sqrt{R^2 - d^2}\).

**Шаг 5. Шаблон оформления 2-балльной задачи.**

```
1. Вижу: [сфера + куб/тетраэдр/конус]
2. Ключевая фраза: [проходит через вершины / касается граней]
3. Это: [описанная / вписанная] сфера, ищу [R / r]
4. Применяю формулу из таблицы выше.
5. Подставляю числовые значения.
6. Ответ: [число с единицами]
```

**Замечание.** Если задача просит **площадь** или **объём** самой сферы, а не её радиус:
- \(S_{\\text{сф}} = 4\\pi R^2\).
- \(V_{\\text{сф}} = \\dfrac{4}{3}\\pi R^3\).

---

"""

if OLD_MARKER in content:
    content = content.replace(OLD_MARKER, NEW_INSERT + OLD_MARKER, 1)
    print("FIX: дерево решений добавлено.")
else:
    print("FIX: маркер 14.8 не найден.")

with io.open(FILE, 'w', encoding='utf-8') as f:
    f.write(content)
print(f"\nFinal length: {len(content)} chars")
