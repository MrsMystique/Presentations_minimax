// Применяет правку 5 к v2.2: разнообразить мостики
const fs = require('fs');
const path = require('path');

const filePath = path.join(
  'C:\\Users\\admin\\Desktop\\математика мои слайды\\Minimax',
  'Чистовые слайды v2.2 — Проценты, пропорции, масштаб.md'
);

let content = fs.readFileSync(filePath, 'utf8');

// Мостик 1 (после Блока 1, перед Блоком 2) — оставляем как есть (это классическая структура)
const old1 = [
  '## Мостик. От пропорции к зависимостям',
  '',
  '> 🎯 Ты освоил пропорцию и её главное свойство — равенство произведений крест-накрест.',
  '>',
  '> Следующий блок покажет, как применять пропорцию в задачах с зависимостями. Это арифметические задачи, где две величины двигаются вместе (или наоборот — одна растёт, другая падает). Пропорция — твой главный инструмент.'
].join('\n');

const new1 = [
  '## Мостик. От пропорции к зависимостям',
  '',
  '> 🎯 Ты освоил пропорцию и её главное свойство — равенство произведений крест-накрест.',
  '>',
  '> **Что ты теперь умеешь:** записывать пропорцию из условия, проверять её наискосок, находить неизвестный член через закон крест-накрест.',
  '>',
  '> Следующий блок покажет, как применять пропорцию в задачах с зависимостями. Это задачи, где две величины двигаются вместе (или наоборот — одна растёт, другая падает). Пропорция остаётся твоим главным инструментом.'
].join('\n');

if (content.indexOf(old1) === -1) {
  console.log('MOSTIK 1 NOT FOUND');
  process.exit(1);
}
content = content.replace(old1, new1);

// Мостик 2 (после Блока 2, перед Блоком 3) — формат «паспорт темы»
const old2 = [
  '## Мостик. От зависимостей к масштабу',
  '',
  '> 🎯 Ты освоил прямую и обратную зависимость через пропорцию.',
  '>',
  '> Следующий блок покажет, как применять пропорцию в географических задачах. Когда на бумаге нужно показать огромное расстояние в километрах, чертёж уменьшают в определённое число раз. Это и есть масштаб.'
].join('\n');

const new2 = [
  '## Мостик. От зависимостей к масштабу',
  '',
  '> 🎯 Ты освоил прямую и обратную зависимость через пропорцию.',
  '>',
  '> **Паспорт темы «Зависимости»:** прямая «больше → больше», обратная «больше → меньше», метод перевёрнутой пропорции для обратной. Эти три кита остаются с тобой до конца партии.',
  '>',
  '> Следующий блок — про географические задачи. Когда на бумаге нужно показать огромное расстояние в километрах, чертёж уменьшают в определённое число раз. Это называется масштаб.'
].join('\n');

if (content.indexOf(old2) === -1) {
  console.log('MOSTIK 2 NOT FOUND');
  process.exit(1);
}
content = content.replace(old2, new2);

// Мостик 3 (после Блока 3, перед Блоком 4) — формат «предупреждение о частых ошибках»
const old3 = [
  '## Мостик. От масштаба к процентам',
  '',
  '> 🎯 Ты освоил пропорцию через задачи на зависимости и масштаб.',
  '>',
  '> Следующий блок покажет главное применение пропорции — проценты. Процент — это специальная запись дроби, у которой знаменатель всегда 100. Любая процентная задача сводится к пропорции.'
].join('\n');

const new3 = [
  '## Мостик. От масштаба к процентам',
  '',
  '> 🎯 Ты освоил пропорцию через задачи на зависимости и масштаб.',
  '>',
  '> ⚠️ **Важное предупреждение.** В этом блоке тебя ждут задачи, в которых ученики чаще всего путают: что считать частью, что целым, и от чего считать процент. Если на ЦТ ты ошибёшься в типе задачи — ответ будет в десятки раз неправильный. Будь внимателен к типу задачи.',
  '>',
  '> Следующий блок — про проценты. Процент — это специальная запись дроби, у которой знаменатель всегда 100. Любая процентная задача сводится к пропорции.'
].join('\n');

if (content.indexOf(old3) === -1) {
  console.log('MOSTIK 3 NOT FOUND');
  process.exit(1);
}
content = content.replace(old3, new3);

// Мостик 4 (после Блока 4, перед Блоком 5) — формат «главное открытие»
const old4 = [
  '## Мостик. От базовых формул к жизненным задачам',
  '',
  '> 🎯 Ты освоил три базовые формулы процентов.',
  '>',
  '> Следующий блок покажет, как применять их в трёх типовых жизненных сценариях: нахождение процента от числа (скидки), нахождение числа по его проценту (налоги), и сложные проценты (банковские вклады).'
].join('\n');

const new4 = [
  '## Мостик. От базовых формул к жизненным задачам',
  '',
  '> 🎯 Ты только что собрал главный рабочий инструмент партии — три формулы процента.',
  '>',
  '> **Главное открытие:** эти три формулы — не три разных правила. Это одна и та же пропорция, записанная тремя способами (перестановкой членов). Помнишь формулу 1 — остальные получатся из неё алгебраически.',
  '>',
  '> Следующий блок покажет, как применять эти формулы в трёх типовых жизненных сценариях: скидки, налоги и сложные проценты.'
].join('\n');

if (content.indexOf(old4) === -1) {
  console.log('MOSTIK 4 NOT FOUND');
  process.exit(1);
}
content = content.replace(old4, new4);

// Мостик 5 (после Блока 5, перед Блоком 6) — формат «переключатель»
const old5 = [
  '## Мостик. От простых процентов к сложным',
  '',
  '> 🎯 Ты освоил три типа процентных задач — для случая, когда процент применяется ОДИН раз.',
  '>',
  '> Следующий блок — о ситуации, когда процент применяется МНОГО раз подряд (банковский вклад, инфляция, изменение цены каждый год). Это сложные проценты.'
].join('\n');

const new5 = [
  '## Мостик. От простых процентов к сложным',
  '',
  '> 🛑 **Стоп. Переключаем режим мышления.**',
  '>',
  '> До этого момента ты решал задачи, где процент применяется **один раз** (скидка, налог, изменение цены один раз). Следующий блок — про ситуацию, когда процент применяется **много раз подряд**: банковский вклад с капитализацией, инфляция, ежегодное изменение цены. Это сложные проценты. Главное отличие: на втором шаге процент берётся уже от НОВОЙ суммы, а не от исходной.',
  '>',
  '> Готовься: будет формула со степенью. Но степень появится не как магия, а как короткая запись цепочки из \(n\) одинаковых умножений.'
].join('\n');

if (content.indexOf(old5) === -1) {
  console.log('MOSTIK 5 NOT FOUND');
  process.exit(1);
}
content = content.replace(old5, new5);

// Мостик 6 (после Блока 6, перед Блоком 7) — формат «инвентаризация»
const old6 = [
  '## Мостик. От сложных процентов к архиву формул',
  '',
  '> 🎯 Ты освоил все базовые методы работы с процентами.',
  '>',
  '> Следующий блок — Сборник формул. Его можно распечатать и держать под рукой на экзамене.'
].join('\n');

const new6 = [
  '## Мостик. От сложных процентов к архиву формул',
  '',
  '> 🎯 Поздравляю: ты прошёл все методы работы с процентами, пропорциями и масштабом.',
  '>',
  '> **Инвентаризация перед архивом:** у тебя в голове теперь живут 4 крупных инструмента — пропорция с законом наискосок, прямая и обратная зависимость, три формулы процента, формула сложного процента со степенью. Каждый из них решает свой класс задач.',
  '>',
  '> Следующий блок — Сборник формул. Его можно распечатать и держать под рукой на экзамене, чтобы быстро вспомнить, какую формулу к какой задаче применять.'
].join('\n');

if (content.indexOf(old6) === -1) {
  console.log('MOSTIK 6 NOT FOUND');
  process.exit(1);
}
content = content.replace(old6, new6);

fs.writeFileSync(filePath, content, 'utf8');
console.log('OK: правка 5 применена (все 6 мостиков обновлены)');