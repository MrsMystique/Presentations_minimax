// Безопасная замена Слайд 22.7
const fs = require('fs');
const path = 'C:\\Users\\admin\\Desktop\\математика мои слайды\\Minimax\\Чистовые слайды v5.0 — Проценты, пропорции, масштаб.md';
let md = fs.readFileSync(path, 'utf8');

// Найдём Слайд 22.7 по точному маркеру
const slideStartMarker = '### Слайд 22.7.';
const slideEndMarker = '### Слайд 22.8.';
const startIdx = md.indexOf(slideStartMarker);
const endIdx = md.indexOf(slideEndMarker, startIdx);
console.log('Слайд 22.7: ', startIdx, '-', endIdx, ', длина:', endIdx - startIdx);

if (startIdx < 0 || endIdx < 0) {
  console.error('Маркеры не найдены, прерывание');
  process.exit(1);
}

// Найдём внутри Слайда 22.7 блок "**Решение по шагам:**" до "**Ответ:** 76 м."
const solStartMarker = '**Решение по шагам:**';
const solEndMarker = '**Ответ:** 76 м.';
const solStartIdx = md.indexOf(solStartMarker, startIdx);
const solEndIdx = md.indexOf(solEndMarker, solStartIdx) + solEndMarker.length;
console.log('Решение: ', solStartIdx, '-', solEndIdx, ', длина:', solEndIdx - solStartIdx);

// Доп.проверка: решение должно быть внутри Слайда 22.7
if (solStartIdx < startIdx || solEndIdx > endIdx) {
  console.error('Решение вне границ Слайда 22.7, прерывание');
  process.exit(1);
}

const old = md.substring(solStartIdx, solEndIdx);

const neu = [
  '**Решение по шагам:**',
  '',
  '**Шаг 1. Запиши отношение сторон первого участка.** По условию порядок: ширина к длине. Дробь: \\(\\frac{72}{96}\\).',
  '',
  'Сократим её на общий делитель 24, чтобы уменьшить числа для расчёта и увидеть чистую форму: \\(72 \\div 24 = 3\\), \\(96 \\div 24 = 4\\). Форма участка Иванова — 3 к 4.',
  '',
  'Это значит, что на каждые 3 метра ширины приходится ровно 4 метра длины. Этот «ключ формы» — главное, что нужно запомнить из условия участка Иванова.',
  '',
  '**Шаг 2. Найди ширину второго участка.** По условию она на 15 м меньше ширины Иванова: \\(72 - 15 = 57\\) м. Длину участка Петрова обозначим как \\(x\\).',
  '',
  'Отношение его сторон в том же порядке: \\(\\frac{57}{x}\\). Мы пока не знаем длину, поэтому оставляем её как \\(x\\) — это та самая «неизвестная часть», которую нам и нужно найти.',
  '',
  '**Шаг 3. Составь пропорцию.** Так как форма у участков абсолютно одинаковая, приравниваем эти дроби:',
  '',
  '\\[\\frac{3}{4} = \\frac{57}{x}\\]',
  '',
  'Записываем ровно так: левая дробь — форма Иванова (известная после сокращения), правая дробь — форма Петрова (с неизвестной длиной).',
  '',
  '**Шаг 4. Примени закон крест-накрест.** Перемножаем числа наискосок: \\(3 \\cdot x = 4 \\cdot 57\\).',
  '',
  'Отсюда \\(3x = 228\\), и делим обе части на 3: \\(x = \\frac{228}{3} = 76\\) м.',
  '',
  '**Проверка здравым смыслом.** Участок Петрова пропорционально меньше участка Иванова по всем фронтам. Ширина меньше (57 < 72) — значит, и длина обязана стать меньше (76 < 96). Баланс формы сохранён: участок Петрова выглядит как уменьшенная копия участка Иванова.',
  '',
  '**Ответ:** 76 м.'
].join('\n');

md = md.substring(0, solStartIdx) + neu + md.substring(solEndIdx);
fs.writeFileSync(path, md, 'utf8');
console.log('Слайд 22.7 переразбит. Старый:', old.length, ', новый:', neu.length);
