// Финальные правки v3.3: ловушка соль/вода + 11 правок Совета
const fs = require('fs');
const p = 'Чистовые слайды v3.3 — Проценты, пропорции, масштаб.md';
let c = fs.readFileSync(p, 'utf8');
let a = 0;

// === Ловушка соль/вода в Слайде 15.2 ===
const slide152Image = '![Смеси и сплавы: закон суммы долей — два раствора 10% и 20% сливаются, формула m₁ + m₂ = const](assets/smesi-splavy.png)';
const slide152WithTrap = slide152Image + '\n\n> **Возможная ловушка:** не путай «процент соли» с «процентом воды». В растворе соли \\(8\\%\\) означает \\(8\\%\\) массы — это соль. Остальные \\(92\\%\\) — вода. Если в задаче спрашивают «процент воды», ответ будет \\(92\\%\\), а не \\(8\\%\\). Всегда смотри, ЧТО именно в процентах: соль, вода, кислота, спирт, жир, примесь.';
if (c.indexOf(slide152Image) >= 0 && c.indexOf('не путай «процент соли»') < 0) {
  c = c.replace(slide152Image, slide152WithTrap);
  a++;
  console.log('✓ Слайд 15.2: ловушка соль/вода');
}

// === «Почему это важно» в Слайде 15.2 ===
const slide152Start = '### Слайд 15.2. Задачи на смеси и сплавы (растворы)\n\n**Типичная задача.**';
const slide152StartNew = '### Слайд 15.2. Задачи на смеси и сплавы (растворы)\n\n**Почему это важно?** Задачи на растворы, сплавы и смеси — обязательный тип на ЦТ и ЕГЭ. В быту это раствор соли или уксуса, в химии — концентрация, в экономике — купажирование. Умение видеть в задаче три сущности (масса вещества, масса растворителя, процент) — навык, который переносится на любую задачу про «сколько одного в другом».\n\n**Типичная задача.**';
if (c.indexOf(slide152Start) >= 0 && c.indexOf('Задачи на растворы, сплавы') < 0) {
  c = c.replace(slide152Start, slide152StartNew);
  a++;
  console.log('✓ Слайд 15.2: Почему это важно');
}

// === «Как думать» в Слайдах 12, 15.2, 24.4, 24.8 ===
// Используем replace — НЕ indexOf + substring

// Слайд 12: добавить «Как думать» после ловушки
const slide12EndOld = '> Возможная ловушка:';
const slide12EndNew = `**Как думать при новой задаче «найти процент от числа»:**

1. **Найди целое** (то, от чего считаем) и **процент**.
2. **Запиши формулу:** часть = целое × (процент / 100).
3. **Подставь и считай.** Если процент дробный (12,5%) — умножай на десятичную дробь (0,125).

` + slide12EndOld;
if (c.indexOf('### Слайд 12.') >= 0) {
  // Найдём конец Слайда 12
  const idx12 = c.indexOf('### Слайд 12.');
  const idx13 = c.indexOf('### Слайд 13.');
  if (idx13 < 0) {
    console.log('  Слайд 13 не найден');
  } else {
    const slide12 = c.substring(idx12, idx13);
    const newSlide12 = slide12.replace('> Возможная ловушка:', slide12EndNew);
    if (newSlide12 !== slide12) {
      c = c.substring(0, idx12) + newSlide12 + c.substring(idx13);
      a++;
      console.log('✓ Слайд 12: Как думать');
    }
  }
}

// Слайд 15.2: добавить «Как думать» после ловушки
const slide152EndOld = 'Всегда смотри, ЧТО именно в процентах: соль, вода, кислота, спирт, жир, примесь.';
const slide152EndNew = slide152EndOld + `

**Как думать при новой задаче на смеси:**

1. **Найди, что считаем «целым»** — масса раствора или масса сплава. Всегда в килограммах или граммах (не в процентах).
2. **Найди «часть»** — масса чистого вещества (соли, кислоты, металла). Это масса × процент.
3. **Запиши равенство:** масса_части_1 + масса_части_2 = масса_части_общая. Масса_целого_1 + масса_целого_2 = масса_целого_общая.
4. **Примени формулу процента:** новый_процент = (общая_часть × 100) / общее_целое.`;
if (c.indexOf(slide152EndOld) >= 0 && c.indexOf('Найди, что считаем «целым»') < 0) {
  c = c.replace(slide152EndOld, slide152EndNew);
  a++;
  console.log('✓ Слайд 15.2: Как думать');
}

// Слайд 24.4: добавить «Как думать»
const slide244EndOld = '> Возможная ловушка: ученик думает «меньше рабочих → больше дней, значит отвечу что-то большое типа 20».';
const slide244EndNew = slide244EndOld + `

**Как думать при новой задаче на зависимость:**

1. **Пойми смысл задачи:** что происходит с одной величиной, когда другая растёт? Больше → больше (прямая) или больше → меньше (обратная)?
2. **Запиши пропорцию** по выбранному типу. Прямая: \\(A_1/B_1 = A_2/B_2\\). Обратная: \\(A_1/B_2 = A_2/B_1\\) (одна колонка перевёрнута).
3. **Реши через закон наискосок.**`;
if (c.indexOf(slide244EndOld) >= 0 && c.indexOf('Пойми смысл задачи: что происходит') < 0) {
  c = c.replace(slide244EndOld, slide244EndNew);
  a++;
  console.log('✓ Слайд 24.4: Как думать');
}

// Слайд 24.8: добавить «Как думать»
const slide248EndOld = '> Возможная ловушка:';
if (c.indexOf('### Слайд 24.8.') >= 0) {
  const idx248 = c.indexOf('### Слайд 24.8.');
  const idx249 = c.indexOf('### Слайд 24.9.');
  if (idx249 > 0) {
    const slide248 = c.substring(idx248, idx249);
    if (slide248.indexOf('Новый блок для 24.8 Как думать') < 0) {
      const newSlide248 = slide248.replace('> Возможная ловушка:', `**Как думать при новой задаче на переливание:**

1. **Определи направление** переливания: из какого сосуда в какой.
2. **Запиши, сколько процентов** ушло из одного сосуда и сколько пришло в другой.
3. **Запиши равенство** массы или объёма в каждом сосуде после переливания.

` + '> Возможная ловушка:');
      if (newSlide248 !== slide248) {
        c = c.substring(0, idx248) + newSlide248 + c.substring(idx249);
        a++;
        console.log('✓ Слайд 24.8: Как думать');
      }
    }
  }
}

// === Сборник формул — мини-якоря «Когда применять» ===
// Слайды 18-22
const ykor19 = `

**Когда применять:** задачи, где две величины двигаются вместе (рабочие/время, скорость/путь, цена/количество).`;
const ykor20 = `

**Когда применять:** скидки, налоги, изменение цены, рост населения, наценки — везде, где в задаче есть слово «процент» или знак \\(\\%\\).`;
const ykor21 = `

**Когда применять:** карты, чертежи, модели, планы квартир — везде, где есть отношение расстояния на рисунке к реальному.`;
const ykor22 = `

**Когда применять:** банковские вклады с капитализацией, инфляция, ежегодный рост показателя, инвестиции с реинвестированием.`;
const ykor18 = `

**Когда применять:** любые задачи с двумя парами связанных величин (больше/меньше в одной колонке — больше/меньше в другой, или наоборот).`;

// Слайд 18
const s18End = 'Запиши пропорцию по выбранному типу.';
if (c.indexOf('### Слайд 18.') >= 0 && c.indexOf('любые задачи с двумя парами') < 0) {
  const idx18 = c.indexOf('### Слайд 18.');
  const idx19 = c.indexOf('### Слайд 19.');
  if (idx19 > 0) {
    const slide18 = c.substring(idx18, idx19);
    const newSlide18 = slide18 + ykor18;
    c = c.substring(0, idx18) + newSlide18 + c.substring(idx19);
    a++;
    console.log('✓ Слайд 18: Когда применять');
  }
}

// Слайд 19
if (c.indexOf('### Слайд 19.') >= 0 && c.indexOf('рабочие/время, скорость/путь') < 0) {
  const idx19 = c.indexOf('### Слайд 19.');
  const idx20 = c.indexOf('### Слайд 20.');
  if (idx20 > 0) {
    const slide19 = c.substring(idx19, idx20);
    const newSlide19 = slide19 + ykor19;
    c = c.substring(0, idx19) + newSlide19 + c.substring(idx20);
    a++;
    console.log('✓ Слайд 19: Когда применять');
  }
}

// Слайд 20
if (c.indexOf('### Слайд 20.') >= 0 && c.indexOf('наценки — везде') < 0) {
  const idx20 = c.indexOf('### Слайд 20.');
  const idx21 = c.indexOf('### Слайд 21.');
  if (idx21 > 0) {
    const slide20 = c.substring(idx20, idx21);
    const newSlide20 = slide20 + ykor20;
    c = c.substring(0, idx20) + newSlide20 + c.substring(idx21);
    a++;
    console.log('✓ Слайд 20: Когда применять');
  }
}

// Слайд 21
if (c.indexOf('### Слайд 21.') >= 0 && c.indexOf('планы квартир') < 0) {
  const idx21 = c.indexOf('### Слайд 21.');
  const idx22 = c.indexOf('### Слайд 22.');
  if (idx22 > 0) {
    const slide21 = c.substring(idx21, idx22);
    const newSlide21 = slide21 + ykor21;
    c = c.substring(0, idx21) + newSlide21 + c.substring(idx22);
    a++;
    console.log('✓ Слайд 21: Когда применять');
  }
}

// Слайд 22
if (c.indexOf('### Слайд 22.') >= 0 && c.indexOf('инвестиции с реинвестированием') < 0) {
  const idx22 = c.indexOf('### Слайд 22.');
  const idx23 = c.indexOf('### Слайд 23.');
  if (idx23 > 0) {
    const slide22 = c.substring(idx22, idx23);
    const newSlide22 = slide22 + ykor22;
    c = c.substring(0, idx22) + newSlide22 + c.substring(idx23);
    a++;
    console.log('✓ Слайд 22: Когда применять');
  }
}

console.log('\nApplied: ' + a);
fs.writeFileSync(p, c, 'utf8');
