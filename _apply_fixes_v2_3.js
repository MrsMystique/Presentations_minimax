// Применяет ВСЕ 9 правок Совета (HIGH + MEDIUM + LOW) к v2.2 → v2.3
const fs = require('fs');
const path = require('path');

const filePath = path.join(
  'C:\\Users\\admin\\Desktop\\математика мои слайды\\Minimax',
  'Чистовые слайды v2.2 — Проценты, пропорции, масштаб.md'
);

let content = fs.readFileSync(filePath, 'utf8');

// === ПРАВКА 1 (HIGH): Слайд 6 — добавить пояснение про 1:X ===
const old6 = 'Масштаб всегда записывается как \\(1 : X\\), где \\(X\\) — во сколько раз реальный размер больше размера на бумаге. Например, \\(1 : 100\\) означает, что 1 см на бумаге = 100 см в реальности (1 метр).';
const new6 = 'Масштаб всегда записывается как \\(1 : X\\), где \\(X\\) — во сколько раз реальный размер больше размера на бумаге. Например, \\(1 : 100\\) означает, что **1 единица на бумаге = \\(X\\) единиц в реальности** (то есть 1 см на бумаге = 100 см в реальности, или 1 метр).';

if (content.indexOf(old6) === -1) {
  console.log('FIX 1 (HIGH, Слайд 6) NOT FOUND');
  process.exit(1);
}
content = content.replace(old6, new6);
console.log('FIX 1 (HIGH) applied');

// === ПРАВКА 2 (MEDIUM 1): Слайд 9, Формула 3 — убрать дублирование A≠0 ===
const old9 = '\\[ p = \\frac{B \\cdot 100}{A}, \\quad A \\neq 0, \\quad A \\neq 0 \\]';
const new9 = '\\[ p = \\frac{B \\cdot 100}{A}, \\quad A \\neq 0 \\]';

if (content.indexOf(old9) === -1) {
  console.log('FIX 2 (MEDIUM, Слайд 9 Формула 3) NOT FOUND');
  process.exit(1);
}
content = content.replace(old9, new9);
console.log('FIX 2 (MEDIUM 1) applied');

// === ПРАВКА 3 (MEDIUM 2): Слайд 13 — добавить вывод k = 1 + p/100 ===
const old13a = '**Почему именно формула степени?** Каждый год сумма умножается на один и тот же множитель \\(k = 1 + \\frac{p}{100}\\). Получается цепочка умножений: каждый следующий год — это предыдущий, умноженный на \\(k\\).';
const new13a = '**Откуда берётся множитель \\(k\\)?** Каждый год мы оставляем \\(100\\%\\) старой суммы и добавляем \\(p\\%\\) новых процентов. Итого за год получаем \\((100 + p)\\%\\). Чтобы перевести проценты в десятичную дробь, делим на 100: \\((100 + p)\\% = \\frac{100 + p}{100} = 1 + \\frac{p}{100}\\). Этот множитель обозначают буквой \\(k\\).\n\n**Почему именно формула степени?** Каждый год сумма умножается на один и тот же множитель \\(k = 1 + \\frac{p}{100}\\). Получается цепочка умножений: каждый следующий год — это предыдущий, умноженный на \\(k\\).';

if (content.indexOf(old13a) === -1) {
  console.log('FIX 3 (MEDIUM, Слайд 13) NOT FOUND');
  process.exit(1);
}
content = content.replace(old13a, new13a);
console.log('FIX 3 (MEDIUM 2) applied');

// === ПРАВКА 4 (MEDIUM 3): Слайд 17 — добавить пояснение «Числовой разбор» ===
const old17 = '| Часть от целого | \\(\\text{Часть} = A \\cdot \\frac{p}{100}\\) | \\(15\\%\\) от \\(200\\):<br>\\(\\text{Часть} = 200 \\cdot \\frac{15}{100} = 30\\) |';
const new17 = '| Часть от целого | \\(\\text{Часть} = A \\cdot \\frac{p}{100}\\) | \\(15\\%\\) от \\(200\\):<br>**Числовой разбор формулы** (буквы → числа):<br>\\(\\text{Часть} = 200 \\cdot \\frac{15}{100} = 30\\) |';

if (content.indexOf(old17) === -1) {
  console.log('FIX 4 (MEDIUM, Слайд 17) NOT FOUND');
  process.exit(1);
}
content = content.replace(old17, new17);
console.log('FIX 4 (MEDIUM 3) applied');

// === ПРАВКА 5 (MEDIUM 4): Слайд 21.4 — расшифровать перевёрнутую пропорцию ===
const old21_4 = 'Составляем перевёрнутую пропорцию: \\(\\frac{5}{3} = \\frac{x}{12}\\). Отсюда по закону крест-накрест (перемножаем числа наискосок) переходим к уравнению в одну строчку:';
const new21_4 = '**Что значит «перевёрнутая»?** Если 5 маляров → 12 дней, то 3 маляра → ? дней. При обратной зависимости больше рабочих соответствует меньшему количеству дней. Поэтому пропорция переворачивается: первая дробь становится \\(\\frac{5}{3}\\) (больше/меньше), а вторая \\(\\frac{x}{12}\\) (неизвестное/больше).\n\nСоставляем перевёрнутую пропорцию: \\(\\frac{5}{3} = \\frac{x}{12}\\). Отсюда по закону крест-накрест (перемножаем числа наискосок) переходим к уравнению в одну строчку:';

if (content.indexOf(old21_4) === -1) {
  console.log('FIX 5 (MEDIUM, Слайд 21.4) NOT FOUND');
  process.exit(1);
}
content = content.replace(old21_4, new21_4);
console.log('FIX 5 (MEDIUM 4) applied');

// === ПРАВКА 6 (LOW 1): Мостик 1 — сделать уникальным ===
const old_m1 = '## Мостик. От пропорции к зависимостям\n\n> 🎯 Ты освоил пропорцию и её главное свойство — равенство произведений крест-накрест.\n>\n> **Что ты теперь умеешь:** записывать пропорцию из условия, проверять её наискосок, находить неизвестный член через закон крест-накрест.\n>\n> Следующий блок покажет, как применять пропорцию в задачах с зависимостями. Это задачи, где две величины двигаются вместе (или наоборот — одна растёт, другая падает). Пропорция остаётся твоим главным инструментом.';
const new_m1 = '## Мостик. От пропорции к зависимостям\n\n> 🧱 **Твой первый кирпич партии готов.**\n>\n> Пропорция — это фундамент, на котором стоит вся партия. Ты научился видеть равенство двух дробей, проверять его наискосок, и доставать из неё неизвестное число через закон крест-накрест.\n>\n> **Что ты теперь умеешь:** записывать пропорцию из условия, проверять её наискосок, находить неизвестный член через закон крест-накрест.\n>\n> Следующий блок покажет, как применять пропорцию в задачах с зависимостями. Это задачи, где две величины двигаются вместе (или наоборот — одна растёт, другая падает). Пропорция остаётся твоим главным инструментом.';

if (content.indexOf(old_m1) === -1) {
  console.log('FIX 6 (LOW, Мостик 1) NOT FOUND');
  process.exit(1);
}
content = content.replace(old_m1, new_m1);
console.log('FIX 6 (LOW 1) applied');

// === ПРАВКА 7 (LOW 2): Слайд 10 — унифицировать пример с курткой ===
const old10 = 'Пример. Куртка стоит 5000 руб, скидка \\(20\\%\\). Скидка = \\(5000 \\cdot 0{,}2 = 1000\\) руб. Цена со скидкой = \\(5000 - 1000 = 4000\\) руб. Или сразу: \\(5000 \\cdot 0{,}8 = 4000\\).';
const new10 = 'Пример. Шоколадка стоит 50 руб (за одну штуку), скидка \\(20\\%\\). Скидка = \\(50 \\cdot 0{,}2 = 10\\) руб. Цена со скидкой = \\(50 - 10 = 40\\) руб. Или сразу: \\(50 \\cdot 0{,}8 = 40\\).';

if (content.indexOf(old10) === -1) {
  console.log('FIX 7 (LOW, Слайд 10) NOT FOUND');
  process.exit(1);
}
content = content.replace(old10, new10);
console.log('FIX 7 (LOW 2) applied');

// Также нужно унифицировать сумму 5000 в разборе двойной скидки
const old10b = 'Товар стоил 5000 руб. Сначала скидка \\(20\\%\\), потом ещё скидка \\(10\\%\\) от новой цены. Сколько стоит товар?\n\n**Шаг 1.** Первая скидка: \\(5000 \\cdot 0{,}8 = 4000\\) руб.\n**Шаг 2.** Вторая скидка от НОВОЙ цены: \\(4000 \\cdot 0{,}9 = 3600\\) руб.\n**Итог:** 3600 руб.\n\nРеальная общая скидка: \\(\\frac{5000 - 3600}{5000} = \\frac{1400}{5000} = 28\\%\\), а не \\(30\\%\\)';
const new10b = 'Товар стоил 50 руб. Сначала скидка \\(20\\%\\), потом ещё скидка \\(10\\%\\) от новой цены. Сколько стоит товар?\n\n**Шаг 1.** Первая скидка: \\(50 \\cdot 0{,}8 = 40\\) руб.\n**Шаг 2.** Вторая скидка от НОВОЙ цены: \\(40 \\cdot 0{,}9 = 36\\) руб.\n**Итог:** 36 руб.\n\nРеальная общая скидка: \\(\\frac{50 - 36}{50} = \\frac{14}{50} = 28\\%\\), а не \\(30\\%\\)';

if (content.indexOf(old10b) !== -1) {
  content = content.replace(old10b, new10b);
  console.log('FIX 7b (LOW 2, разбор двойной скидки) applied');
}

console.log('FIX 7 (LOW 2) applied');

// === ПРАВКА 8 (LOW 3): Слайд 21.6 — унифицировать ответ ===
const old21_6 = '**Ответ:** 62985,60 руб (или 62 985,60 руб).';
const new21_6 = '**Ответ:** \\(62\\)\\nbsp985,60 руб.';

if (content.indexOf(old21_6) === -1) {
  console.log('FIX 8 (LOW, Слайд 21.6) NOT FOUND');
  process.exit(1);
}
content = content.replace(old21_6, new21_6);
console.log('FIX 8 (LOW 3) applied');

// === ПРАВКА 9 (LOW 4): Мостик 5 — заменить «не как магия» ===
const old_m5 = 'Готовься: будет формула со степенью. Но степень появится не как магия, а как короткая запись цепочки из (n) одинаковых умножений.';
const new_m5 = 'Готовься: будет формула со степенью. Но степень появится не как готовый результат, а как короткая запись цепочки из (n) одинаковых умножений.';

if (content.indexOf(old_m5) === -1) {
  console.log('FIX 9 (LOW, Мостик 5) NOT FOUND');
  process.exit(1);
}
content = content.replace(old_m5, new_m5);
console.log('FIX 9 (LOW 4) applied');

// === ПРАВКА 10 (LOW 5): Слайд 17, Формула 3 — добавить A≠0 ===
const old17_f3 = '| Процент по части | \\(p = \\frac{B \\cdot 100}{A}\\) | 30 от 200 = \\(15\\%\\) |';
const new17_f3 = '| Процент по части | \\(p = \\frac{B \\cdot 100}{A}, \\quad A \\neq 0\\) | 30 от 200 = \\(15\\%\\) |';

if (content.indexOf(old17_f3) === -1) {
  console.log('FIX 10 (LOW, Слайд 17 Формула 3) NOT FOUND');
  process.exit(1);
}
content = content.replace(old17_f3, new17_f3);
console.log('FIX 10 (LOW 5) applied');

// === ДОПОЛНИТЕЛЬНО: убираем эмодзи 🚨 (Olga не любит излишеств) - уже не нужно, оставляем ===
// === СОХРАНЕНИЕ ===
const newPath = filePath.replace('v2.2', 'v2.3');
fs.writeFileSync(newPath, content, 'utf8');
console.log('\n=== ALL FIXES APPLIED. Saved to v2.3 ===');