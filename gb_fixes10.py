#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import io
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')

FILE = r"C:\Users\admin\Desktop\математика мои слайды\Minimax\Геометрия_11_Полный_гайд.md"

with io.open(FILE, 'r', encoding='utf-8') as f:
    content = f.read()

# ============================================================
# FIX 1: Убрать миф в Главном мосте
# ============================================================
OLD_MYTH = """3. **Площадь круга** \(\pi r^2\) → становится **сферическая поверхность** \(4\pi r^2\) (тот же πr² с коэффициентом)."""

NEW_MYTH = """3. **Площадь круга** \(\pi r^2\) → **сферическая поверхность** \(4\pi r^2\) — **новая 3D-формула**, НЕ прямой перенос из планиметрии. Вывод \(S_{\\text{сферы}} = 4\\pi r^2\) требует либо математического анализа (через производную \(V_{\\text{шар}} = \\frac{4}{3}\\pi r^3\), см. Секцию 10.4), либо принимается как геометрический факт. В школьном курсе **принимается** без вывода."""

if OLD_MYTH in content:
    content = content.replace(OLD_MYTH, NEW_MYTH, 1)
    print("FIX 1 (Главный мост миф): применён.")
else:
    print("FIX 1: не найдено.")

# ============================================================
# FIX 2: Удалить/исправить Шаг 1 в 14.3
# Старый текст содержит Δ₂ с гипотенузой R и катетом R — бессмысленно
# ============================================================
OLD_STEP = """**Шаг 1.** Проведём два прямоугольных треугольника:
- Δ₁ — из центра основания \(O\) в вершину пирамиды \(S\): катеты \(h\) (высота) и \(R_{\\text{осн}}\) (радиус основания), гипотенуза \(OS\).
- Δ₂ — из центра сферы \(O_{\\text{сф}}\) в вершину пирамиды \(S\): катеты \(h - x\) (расстояние от центра сферы до вершины) и \(R\) (радиус сферы), гипотенуза тоже \(R\).

**Шаг 2.** Заметим: расстояние от центра сферы до любой точки окружности основания = радиусу сферы \(R\). Применяем теорему Пифагора в Δ с вершинами \(O_{\\text{сф}}\), \(O\), точкой на окружности основания. Катеты: \(x\) (расстояние \(O O_{\\text{сф}}\) вдоль оси) и \(R_{\\text{осн}}\). Гипотенуза: \(R\). Итого: \(R^2 = R_{\\text{осн}}^2 + x^2\)."""

NEW_STEP = """**Шаг 1.** Построим прямоугольный Δ с вершинами \(O_{\\text{сф}}\) (центр описанной сферы), \(O\) (центр основания) и \(B\) (точка на окружности основания). Этот Δ прямоугольный (прямой угол при \(O\), так как \(O_{\\text{сф}} O\) ⊥ основанию). Катеты:
- \(O O_{\\text{сф}} = x\) (расстояние от центра основания до центра сферы вдоль оси).
- \(OB = R_{\\text{осн}}\) (радиус описанной окружности основания).
Гипотенуза: \(O_{\\text{сф}} B = R\) (радиус описанной сферы, так как \(B\) — вершина пирамиды, лежащая на сфере).

**Шаг 2.** По теореме Пифагора: \(R^2 = R_{\\text{осн}}^2 + x^2\). Одновременно: расстояние от центра сферы до вершины пирамиды \(S\) = радиусу сферы \(R\), т.е. \(R = h - x\)."""

if OLD_STEP in content:
    content = content.replace(OLD_STEP, NEW_STEP, 1)
    print("FIX 2 (14.3 Шаг 1): применён.")
else:
    print("FIX 2: не найдено.")

# ============================================================
# FIX 3: Добавить вывод формулы r = 3V/S
# Найти первое упоминание "r = 3V/S" в 14.4
# ============================================================
OLD_VS = """Радиус вписанной сферы = отношение **тройного объёма** к **полной поверхности**:
\[
r = \frac{3V}{S_{\text{полн}}}.
\]"""

NEW_VS = """Радиус вписанной сферы = отношение **тройного объёма** к **полной поверхности**:
\[
r = \frac{3V}{S_{\text{полн}}}.
\]

**Вывод.** Соединим центр вписанной сферы \(O_{\text{вп}}\) со всеми вершинами многогранника. Он разбивается на **\(n\) маленьких пирамид**, где \(n\) — число граней, каждая с высотой \(r\) (перпендикуляр от \(O_{\text{вп}}\) к грани) и основанием — соответствующей гранью площади \(S_i\). Сумма объёмов: \(\sum_{i=1}^n \frac{1}{3} S_i \cdot r = \frac{r}{3} \sum_{i=1}^n S_i = \frac{r}{3} S_{\text{полн}}\). Эта сумма равна объёму многогранника \(V\) (все пирамиды вместе заполняют его целиком). Значит: \(\frac{r}{3} S_{\text{полн}} = V\), откуда \(r = \dfrac{3V}{S_{\text{полн}}}\). ∎"""

if OLD_VS in content:
    content = content.replace(OLD_VS, NEW_VS, 1)
    print("FIX 3 (вывод r = 3V/S): применён.")
else:
    print("FIX 3: не найдено.")

# Write back
with io.open(FILE, 'w', encoding='utf-8') as f:
    f.write(content)
print(f"\nFinal length: {len(content)} chars")
