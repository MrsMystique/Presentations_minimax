// Чистит остаточные запрещённые слова в v2.3
const fs = require('fs');
const path = require('path');

const filePath = path.join(
  'C:\\Users\\admin\\Desktop\\математика мои слайды\\Minimax',
  'Чистовые слайды v2.3 — Проценты, пропорции, масштаб.md'
);

let content = fs.readFileSync(filePath, 'utf8');

const replacements = [
  // 1. Заголовок Слайда 21.7
  ['### Слайд 21.7. Геометрическая пропорциональность (участки)',
   '### Слайд 21.7. Пропорциональность через прямоугольники (участки)'],
  // 2. Alt картинки
  ['![Геометрический смысл пропорции: закон сохранения формы при изменении масштаба](assets/maschtab-proporcii.png)',
   '![Пропорциональность через прямоугольники: закон сохранения формы при изменении размера](assets/maschtab-proporcii.png)'],
  // 3. "Геометрическая интуиция" в Слайде 21.7
  ['**🪞 Геометрическая интуиция.** Если нарисовать прямоугольник Иванова (72×96) и внутри него маленький прямоугольник Петрова (57×76), у них будет одинаковое отношение сторон: 57/76 = 72/96 = 3/4. Это и есть пропорциональность: при изменении размера форма сохраняется.',
   '**🪞 Интуиция через прямоугольник.** Если нарисовать прямоугольник Иванова (72×96) и внутри него маленький прямоугольник Петрова (57×76), у них будет одинаковое отношение сторон: 57/76 = 72/96 = 3/4. Это и есть пропорциональность: при изменении размера форма сохраняется.'],
  // 4. "геометрический смысл уравнения" в Слайде 21.8
  ['Это и есть геометрический смысл уравнения.',
   'Это и есть визуальный смысл уравнения.'],
  // 5. Footer v2.3 — замены
  ['геометрическая пропорциональность участков (21.7)',
   'пропорциональность через прямоугольники (21.7)'],
  // 6. Footer v2.3 — замена "магия"
  ['замена «не как магия»',
   'замена разговорной фразы на математически корректную'],
];

let applied = 0;
for (const [oldStr, newStr] of replacements) {
  if (content.indexOf(oldStr) === -1) {
    console.log('NOT FOUND:', oldStr.substring(0, 80));
    continue;
  }
  content = content.replace(oldStr, newStr);
  applied++;
}
console.log('Applied ' + applied + ' replacements of ' + replacements.length);

fs.writeFileSync(filePath, content, 'utf8');